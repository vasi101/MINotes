[package]
name = "minotes"
version = "0.1.0"
description = "Mi Notes inspired desktop notebook"
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
